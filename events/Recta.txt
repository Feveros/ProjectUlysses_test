add_namespace = recta

country_event = { ### Recta is born
	id = recta.1
	title = recta.1.t
	desc = recta.1.d
	picture = GFX_report_event_belka_meme_read_write

	is_triggered_only = yes

	option = {
		name = recta.1.a
		news_event = { id = ac_news_event.3 days = 1 }
		hidden_effect = {
			GEB = {
				country_event = utility.1
			}
		}
		REC = {
			inherit_technology = GEB 
			transfer_state = 345
			transfer_state = 300
			transfer_state = 316
		}
		if = {
			limit = {
				GEB = { owns_state = 293 }
			}
			REC = { transfer_state = 293 }
		}
		add_popularity = {
			ideology = communism
			popularity = -20
		}
		hidden_effect = {
			REC = { load_oob = "REC_land" }
		}
	}
}