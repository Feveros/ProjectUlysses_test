add_namespace = trigger_event

country_event = { ### USTIO RESURCES - EVENT CHAIN FOR BELKAN WAR
	id = trigger_event.1
	title = trigger_event.1.t
	desc = trigger_event.1.d
	picture = GFX_report_event_belka_meme_read_write
	fire_only_once = yes
	is_triggered_only = yes
	option = {
		name = trigger_event.1.a
		if = {
			limit = {
				AND = {
					UST = { exists = yes }
				}
			}
			UST = { country_event = { id = trigger_event.2 days = 1 }}
		}
	}
}

country_event = { ### USTIO RESURCES - EVENT CHAIN FOR BELKAN WAR
	id = trigger_event.2
	title = trigger_event.2.t
	desc = trigger_event.2.d
	picture = GFX_report_event_belka_meme_read_write
	fire_only_once = yes
	is_triggered_only = yes
	option = {
		name = trigger_event.2.a
		if = {
			limit = {
				AND = {
					BLK = { exists = yes }
					BLK = { has_government = fascism }
				}
			}
			BLK = { country_event = { id = belka_dlp_war.1 days = 1 }}
		}
	}
}