FAT_defend_vs_belka  = {
	allowed = {
		original_tag = FAT
	}

	enable = {
		has_war_with = BLK
		BLK = { is_ai = yes }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control

		tag = BLK

		#ratio = 0.5

		priority = 100
		ordertype = front
		execution_type = careful
	}
}