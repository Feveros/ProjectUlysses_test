# Scripted AI desire for peace actions.
# See documentation.info file for more detailed scripting information.

peace_ai_desires = {
    # Don't annex Ustio or Ratio if not fascist
    RAT_dont_annex_all_ustio = {
        peace_action_type = { take_states }
        enable = {
            FROM.FROM.FROM = {
                NOT = {
                    is_core_of = RAT
                }
            }
            ROOT = {
                original_tag = RAT
            }
        }
        ai_desire = -100
    }
}
